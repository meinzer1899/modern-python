# Additional Notes to Server/Subscriber web app

## Client

* Use `curl 'http://localhost:8080/'` to expect to get output from @route('/')
* Use `curl 'http://localhost:8080/' -v` to get header files from server
